# 合成タグでの通し実行（Stage 1〜4）

データ: `tools/.build/labels`

6記号を含むタグを合成し、検出→切り出し→分類→射影まで通した。
`tools/synth_label.py` のタグには、実物と同じように文字（素材表示・ブランド・注意書き）が入る。
文字を入れていない `tools/synth_tag.py` では切り出しが100%だったのに、実機では6個中2〜3個しか
取れなかった。文字が入っているかどうかで結果がまったく変わるので、こちらを主の指標とする。
ぼけ・コントラスト・ノイズ・回転は s2 相当に固定。

**これは実写での性能ではない。** 実物のタグでの評価は未実施。

| 1記号のpx | 帯 | 記号を過不足なく切り出せた帯 | 確定できた率 | 確定分の正解率 | 確定分の誤り |
|---|---|---|---|---|---|
| 200px | 10 | 90% (9/10) | 80% (48/60) | 93.8% (45/48) | 3 |
| 150px | 10 | 100% (10/10) | 87% (52/60) | 90.4% (47/52) | 5 |
| 120px | 10 | 100% (10/10) | 93% (56/60) | 98.2% (55/56) | 1 |
| 100px | 10 | 100% (10/10) | 92% (55/60) | 100.0% (55/55) | 0 |
| 80px | 10 | 100% (10/10) | 82% (49/60) | 93.9% (46/49) | 3 |

## 実写10枚での通し評価（2026-08-20）

`eval/truth.json`（写真を拡大して1枚ずつ目視で起こした正解、63記号）と
`tools/score_real.cjs` で突き合わせた。手集計ではなく機械で採点する。

| 手を入れた順 | 切り出せた記号 | 確定 | 正解と一致 | 再現率 |
|---|---|---|---|---|
| 変更前 | 36/63 | 18 | 15 | 23.8% |
| ＋候補の高さ上限を外す | 58/63 | 28 | 22 | 34.9% |
| ＋インクの明暗を自動判定 | 62/63 | 41 | 31 | 49.2% |
| ＋ぼかしで探して原画で読む | 64/63 | 44 | 33 | **52.4%** |

（切り出し 64 は過検出を含む。分母は正解の 63。）

### ぼかしの半径をどう決めたか

固定半径で振ると、一致数は r=1 で26、r=3 で32、r=6 で33、r=8 で23。
3〜6 が平らな山で、その外に出ると落ちる。中央値フィルタも試したが
（r=4 で28）、箱ぼかしのほうが良く、しかも累積和で1画素あたり定数時間。
**半径は写真の画素数ではなく記号の高さに比例させる**（記号の高さ÷60）。
除数を 25〜100 で振っても一致は 30〜32 で平ら。値そのものより
「その辺ならどこでもいい」ことのほうが重要。

### 残っている取りこぼし

`445`（日陰つり干し）4件、`425`（日陰平干し）3件、`152` 3件、`510` 3件。
**ぼかしが細部を溶かす**のが原因で、日陰の斜線と下線が消えて
`425 -> 420`、`152 -> 150` に落ちる。「探すのはぼかし・読むのは原画」を
入れて一部は戻したが、切り出し枠がぼかし側で決まるぶん取り切れていない。

## 「黒は黒く、白は白く」は効かなかった（2026-08-20）

読み取る前に濃淡を強めれば当たるのでは、という案を測った。実写10枚・63記号。

| 前処理 | 正解と一致 |
|---|---|
| なし | **33** |
| 2〜98パーセンタイルで線形に伸ばす | 26 |
| 上を伸ばしてからS字（gain 4 / 8 / 14） | 30 / 29 / 27 |
| 照明ムラを除いてからS字（gain 3 / 6 / 10 / 16） | 25 / 27 / 26 / 25 |
| 局所平均と分散で正規化（窓 1/4 / 1/8 / 1/16） | 30 / 17 / 12 |

**11通りすべて悪化した。** 理由ははっきりしている。この工程はもともと
二値化しており、これは濃淡を強める操作の極限（白か黒のどちらかにする）である。
しきい値は Otsu が写真ごとに選び直すので、単調な濃淡カーブをかけても
ヒストグラムごと動くだけで境目は変わらない。増えるのは端で潰れる情報だけ。

作業解像度も同時に測った（長辺 1000 / 1400 / 1800 / 2200 / 2600 px）。
一致は 29 / **33** / 28 / 28 / 27。上げても織り目まで細かく写るだけで得はない。

高解像の写真ほど当たるという印象は正しいが、効いているのは
**インクとその周りの明暗差**であって、これは撮影の条件で決まる。
あとから単調な変換で作り出せる量ではない。

## 線の太さ違いのテンプレートを持つ（2026-08-20）

実写の相関は 0.3〜0.6 で、合成の 0.7〜0.9 に届かない。形が違うのではなく
**布のインクは滲んで太る**のが主因だった。テンプレートのパッチを膨張・収縮
させた変種を持たせ、いちばん合うものを採る。

| 変種（段数） | 実写の一致（63記号中） |
|---|---|
| `[0]`（従来） | 33 |
| `[-1, 0]` | 33 |
| `[-1, 0, 1]` | 32 |
| `[-2, -1, 0, 1]` | 32 |
| `[-1, 0, 1, 2]` | **36** |
| `[-2, 0, 2]` | **36** |
| `[-3, 0, 3]` | **36** |
| `[-4, -2, 0, 2, 4]` | **36** |

**+2段の膨張が入っているかどうかで決まる**。細くする側だけでは効かない。
実物のほうが太い、という一方向の食い違いなので筋が通る。
平らな部分でいちばん軽い `[-2, 0, 2]`（43×3=129枚）を採る。

あわせて、マージン（1位と2位の相関差）の2位を「**別の記号の**中での最高」に
直した。同じ記号の太さ違いが2位に来てもそれは迷いではないので、
マージンを削ってはいけない。

正解が候補の何位にいるか（実写65箱）: 1位 27 -> **33**、2〜3位 16 -> 8。
合成タグは 90.4/98.2/100.0/93.9% -> 91.8/98.2/100.0/93.6% でほぼ不変。
Python とのパリティも 2322件で完全一致のまま（tools/dump_raw.py に同じ
膨張・収縮を移植した）。

## この10枚で測れることの限界

63記号のうち**種類は19しかない**（全43種）。一度も出てこない記号が24種ある。

| 分類 | 出てくる種類 / 全体 |
|---|---|
| 洗濯 | 4/15 |
| 漂白 | 2/3 |
| タンブル乾燥 | 1/3 |
| 自然乾燥 | 2/8 |
| アイロン | 3/5 |
| ドライ | 4/5 |
| ウェット | 3/4 |

しかも19種のうち8種は1枚にしか出てこない。1枚の写り方で数字が動く。
**この集合で測れるのは記号表の半分以下**で、残りは合成データでしか見ていない。
