/**
 * 画面に出すビルド識別子。
 *
 * 「直したのに実機で何も変わらない」が起きたときに、
 * 端末が新しいコードを読んでいるのかキャッシュされた古いバンドルなのかを
 * 推測ではなく目視で判別するために置いている。
 *
 * mobile/ を触って実機で確認してもらうたびに、必ずここを1つ上げること。
 */
export const BUILD = "b17";

/** 何を変えたビルドか。実機で確認してもらうときの目印 */
export const BUILD_NOTE = "記号の形を公式に合わせた";
