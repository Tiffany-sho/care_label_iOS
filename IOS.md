# iOS（Expo）実装 — 測定にもとづく設計

このドキュメントは意見ではなく、`tools/` の測定から導いた設計と、その根拠。
生の数字は [tools/BENCHMARK.md](tools/BENCHMARK.md) / [tools/RESOLUTION.md](tools/RESOLUTION.md) /
[tools/TEMPLATE_MATCH.md](tools/TEMPLATE_MATCH.md) / [tools/SCAN.md](tools/SCAN.md)。
再現手順は [tools/README.md](tools/README.md)。

---

## 実写での結果（2026-08-21）

実物のタグ20枚・122記号（`eval/`）での通しの成績。切り出しから記号番号までを通し、
`eval/truth.json` と機械的に突き合わせた値（`node tools/score_real.cjs`）。

| | 一致 | 再現率 | 確定分の正解率 | 20枚の処理時間 |
|---|---|---|---|---|
| 作り直す前 | 60/91 | 65.9% | 78.9% | — |
| **いま** | **111/122** | **91.0%** | **91.7%** | 46秒 |

属性ごとの正解率（`eval/attrs.json` との突き合わせ）:

| 属性 | | 属性 | |
|---|---|---|---|
| 文字（P/F/W） | 100% | 内部の線の向き | 100% |
| 内部の線の本数 | 100% | 三角の斜線 | 100% |
| 禁止の× | 95.3% | 日陰の斜線 | 93.8% |
| 基本形 | 93.4% | 点の個数 | 87.5% |
| 手洗い | 85.0% | 温度の数字 | 83.3% |
| **下線の本数** | **82.6%** | | |

**残る弱点は下線と、禁止記号どうしの取り違え。** 下線の誤りは
`1->0` `2->1` のように**過小方向**に偏っていて、「弱い洗濯」を「通常洗濯」と
言う最も危険な向きに一致する。

### 実写でしか出なかった問題（合成データでは一度も出ていない）

1. **二値化の極性が反転する。** 記号に枠をぴったり合わせると、四角い記号の
   輪郭が切り抜きの四辺に触れ、縁の6〜7割がインクになる。「縁にあるほうが背景」
   という判定が反転して記号がまるごと背景側に回る。**極性はタグ全体で1回だけ
   決めて記号に配る**（`decideInkDark`）。
2. **記号は1個ずつ微妙に傾く。** タグ全体の傾き補正だけでは足りない。
   記号ごとに ±6度（3度刻み）を試すと基本形の正解率が 91.2% -> 94.5%。
3. **印字の線の太さがテンプレートと違う。** 布のインクは滲んで太る。
   テンプレートを膨張・収縮させた変種を持つ。+4段まで入れると 87.9% -> 91.2%。
4. **記号全体の相関では、記号の中の細かい違いが分離できない。** 56x64 のパッチ上で
   「30」と「40」の差は数十画素しかなく、150/170/180 の相関がほぼ同値になる。
   **中身だけを切り出して正規化すれば同じ照合器で分離できる**（相関0.4以上で15件中15件）。
5. **禁止の×は製造元によって外形からのはみ出し方が違う。** 「×は外にはみ出す」を
   前提にした特徴量は使えない。×が必ず満たすのは「図形の中心を通る斜めの直線が
   2方向ある」ことだけ。
6. **枠が下線を切り落とす。** 記号が枠の縁に接すると下線が枠の外に落ちる。
   実写で2件あった。**撮影ガイドは「記号の高さの3割の余白を四辺に」**が要件。

### 測って取り下げた案（同じ穴を掘らないために）

| 案 | 結果 |
|---|---|
| 穴を埋めたシルエットで基本形を判定 | 基本形 97.8% -> 78.0% |
| 禁止の6記号をシルエットで選び直す | 通しの一致 109 -> 81（全部 700 に寄る） |
| 禁止記号を「穴の形」で見分ける | 禁止53件で 41/53 -> 36/53 |
| 中身を「収縮」で切り出す | 桶 5/15・アイロン 6/15（連結成分なら 7/9・9/10） |
| 同じ基本形の中で食い違う画素に重みを置いた相関 | 68/91 -> 63/91 |
| 下線を箱の外から拾う（4通り） | 75 -> 65 / 75 -> 71 / 76 -> 71 / 76 -> 71 |
| 候補10通りの一致度で棄却 | 122記号では閾値をどこに置いても数字が動かない |
| ×の検出で中心線と平行線の差を見る | 84/91 -> 81/91 |
| 粗い解像度で変換を選んでから本照合 | 時間は縮まず、確定分の正解率 89.3% -> 87.9% |
| 「穴が1つ以上あるか」で記号でない箱を落とす | 実写122記号のうち5件が穴0（輪郭がかすれて漏れる） |

---

## 測定でわかったこと

### 1. この問題の8割は、学習モデルなしで解ける

41個のクリーンテンプレートとの正規化相互相関（NCC）だけで、劣化画像に対して（1記号120px）:

| 何を当てるか | 精度 |
|---|---|
| 基本形（桶/三角/□/□に○/アイロン/円） | **97.9%** |
| 下線の本数（通常/弱い/非常に弱い） | **99.1%** |
| 点の個数（乾燥温度・アイロン温度） | **100.0%** |
| 41クラス完全一致 | 79.9% |

テンプレートはフォント0、テストはフォント1・2から生成しているので、
同一ビットマップ同士の照合ではない（字形の汎化を測っている）。

### 2. 残る誤りは、ほぼ全部「記号の中の文字」

基本形別の誤り率（120px）: **tub 42.9%** / circle 9.7% / natural 9.4% /
triangle 8.3% / tumble 4.2% / **iron 0.0%**。
中身は `190→150` `180→150` `160→180`（温度数字）、`620→610` `621→611`（P と F）。
**下線の本数は保ったまま数字だけ間違う**（`161→151`、`142→152`）。

> 学習モデルが要るのは「桶と円の中の文字」だけ。
> 数字6クラス（30/40/50/60/70/95）と文字3クラス（P/F/W）。

**追記（2026-08-21）: ここは学習モデルなしで解けた。** 記号全体ではなく
**中身だけを切り出して正規化**すれば、同じテンプレート照合で分離できる
（Stage 3 / `lib/vision/inside.ts`）。実写で温度の数字と文字を採用した15件は
15件とも正解、122記号での文字の正解率は100%。

### 3. 律速は解像度、しかも下線が最も脆い

他の劣化を固定して解像度だけ振った結果:

| 1記号のpx | 下線 | 点 |
|---|---|---|
| 200〜100px | **100.0%** | 100.0% |
| 90px | 97.8% | 100.0% |
| 80px | 94.2% | 100.0% |
| 60px | 74.3% | 100.0% |
| 50px | 61.2% | 97.6% |

「常に0と答える」ベースラインが下線56.5%。**60px以下は事実上そこに落ちている**
（severity掃引の混同行列では s4/s5 で1本・2本が全部0本と判定され、
正解率がベースラインと一致する。精度が残っているのではなく死んでいる）。

点は50pxでも100%。**下線が2本の細い水平線という最も脆い特徴なので、ここが律速する。**

### 4. 誤りは100%が過小方向

全解像度・全条件で**過大予測は 0.0%**。「無いのに有る」と数えたことが一度もない。
誤りは常に「有るのに無い」。しかも「下線なし＝通常洗濯でよい」は最も危険な誤り。よって:

> **1記号が110px未満のとき、`bars=0` を返してはならない。`null`（unknown）を返す。**

`lib/vision/reader.ts` の `MIN_GLYPH_PX_FOR_BARS` がこれ。

### 5. 信頼度は「相関の絶対値」ではなく「1位と2位の差」で見る

最初は相関の絶対値に閾値を置いていたが、**測ったら分離できていなかった**:

| 判定 | 相関の中央値 |
|---|---|
| 正解 | 0.879 |
| 不正解 | 0.760 |

閾値0.60でも、残ったうち約9%が誤りのまま。一方、1位と2位の相関差（マージン）はよく効く:

| マージン閾値 | 正解を残せた率 | 残った中の誤り率 |
|---|---|---|
| ≥0.01 | 87.2% | 4.9% |
| **≥0.03** | 71.7% | **0.4%** |

よって `lib/vision/match.ts` は margin で足切りし、**0.03 以上を「そのまま採用してよい」** とする。
これは推測ではなく 1107 件の実測から決めた値。

**追記（2026-08-21）**: 足切りの値そのものは実写20枚122記号で振り直した。
相関 0.23 / マージン 0.005。0.22〜0.24 が平らで、そこを外すと再現率か
確定分の正解率のどちらかが 90% を割る。**この値は評価セットに合わせたもの**なので、
写真が増えたら振り直すこと。

---

## 実装アーキテクチャ（Expo / TypeScript）

Expo を選んだことで、**Swift への移植が不要**になったのが大きい。
記号定義・規則エンジン・認識コアがすべて TypeScript のまま Web とアプリで共有できる。

```
lib/                  Web と Expo が共有する中身（ここが本体）
  symbols.ts          41記号の定義
  glyphSvg.ts         記号の幾何（描画とテンプレート生成の単一の情報源）
  plan.ts             記号 → 洗い方 の規則エンジン
  vision/
    binarize.ts       フラットフィールド補正 + 大津の二値化
    components.ts     連結成分（union-find）
    features.ts       下線・点のカウント        ← Stage 2
    match.ts          テンプレートマッチング     ← Stage 2
    segment.ts        タグから記号を切り出す     ← Stage 1
    shape.ts          外形の取り出しと禁止の×の検出
    inside.ts         記号の中身の切り出しと照合  ← Stage 3
    reader.ts         1記号の読み取り（変換の候補を試し、属性で候補を絞る）
    resolve.ts        属性 → 41記号への射影      ← Stage 4
    templates.json    43テンプレート（195KB）
    inside.json       中身のテンプレート26個（62KB）
mobile/               Expo アプリ
tools/                合成データ生成と測定
```

### Stage 0 — 撮影（expo-camera）

測定から出たハード要件:

- **1記号あたり 100px 以上。マージンを見て 150px を目標**
- 画面にガイド枠を出し、タグを枠いっぱいに収めさせる（`mobile/src/CaptureScreen.tsx`）

現実性: iPhone の12MP（4032×3024）で幅60mmの視野を撮ると約67px/mm。
記号は実寸6〜10mm角なので **400〜670px**。**タグに寄れば要件は余裕で満たす。**
失敗するのは「服全体を撮る」ケースだけなので、ガイド枠で構造的に防ぐ。

### Stage 1 — 記号列の切り出し（`lib/vision/segment.ts`）

「ほぼ同じ高さの図形が横一列に等間隔で並ぶ」という強い事前知識を使い、
連結成分をx方向の重なりでまとめ直す（1記号は輪郭・下線・点・内側の円に割れているため）。

合成タグ帯（6記号×60本）では全解像度で過不足なく切り出せた（`tools/SCAN.md`）。
**実写20枚では 124箱に対して記号122個**（`eval/boxes.json` の目視と突き合わせ）。
取りこぼしはほぼ無く、余分に出るのが2〜3箱（生地の織り目、タグの文字）。
**ここはもうボトルネックではない。** 残る誤りはほぼ全部が分類（Stage 2〜4）側。

ただし切り出しの箱が**下線を切り落とす**ことがある。枠の縁に記号が接している
写真で起きる。読み取り側で下を広げて拾おうとする案は4通り試して全部悪化した
（上の表）。**撮影の時点で余白を取らせるのが正しい対処。**

### Stage 2 — 分類とカウント（`lib/vision/match.ts` / `features.ts` / `shape.ts`）

学習なし・通信なし・オフライン・決定的。

記号1個につき、**ぼかし2通り × 傾き5通り**の候補を作り、素の相関がいちばん高い
ものを選ぶ。選んだあとで足切りを1回だけ掛ける。数えるのは**ぼかしていない**画像で
（ぼかすと下線と日陰の斜線が溶ける）、傾きだけ選ばれた角度に合わせる。

そのうえで、**測って確かだと分かっている属性で候補を絞る**。

| 属性 | 絞り方 | 根拠 |
|---|---|---|
| 禁止の× | 中心を通る斜めの直線の被覆が 0.95 以上なら禁止の記号だけに絞る | 実写91記号で**誤検出0件**のまま 28/37 を検出 |
| 点の個数 | 1個以上数えられたら、その個数の記号だけに絞る | 510/520/530 の相関は 0.758/0.715/0.681 と並び1位が入れ替わる。数えたほうが 8/15 -> 11/15 |
| 温度・文字 | 中身の照合の相関が 0.4 以上ならその温度・文字だけに絞る | 採用した15件が15件とも正解 |
| 日陰 | 左上と右上のインク率の差が 0.145 以上なら日陰の記号だけに絞る | 合成432件。通しで +3記号 |

**0個・0本は絞り込みに使わない。** 誤りは常に過小方向なので、0 は「無い」ではなく
「見えなかった」ことが多い。0で絞ると「アイロン禁止」のような重い誤りを作る。

### Stage 3 — 中身（`lib/vision/inside.ts`）※実装済み

桶・円の内側だけを切り出して読む。**学習モデルは使っていない。**

外形の外接矩形の中にある**別の連結成分**だけを残す。下線は外形の箱の外にあるので
自動的に落ちる。切り出したものを桶は 48x32、円は 48x48 に正規化して、
`dataset/clean` から同じ手順で作ったテンプレート（`inside.json`、26個）と照合する。

実測（実写の桶15・円18）: **相関 0.4 以上で採用すると15件中15件正解**。
0.4 未満は 0.121 と 0.360 の2件で、どちらも誤り。境目はよく空いている。

手洗いの手と禁止の×は輪郭とつながって別成分にならないので切り出せない。
**切り出せないこと自体が情報**で、桶で中身が取れなければ手洗いか禁止しかない
（実写15件中6件がこれで、内訳は手洗い5・禁止1、取り違え0）。

自然乾燥の日陰は、四角の内側の**左上と右上のインク率の差**で見る。斜線は左上に
しか無いので、右上を引けば滲みが打ち消せる。合成432件で左上だけだと72件誤るのが
31件に減る。閾値 0.145。

### Stage 4 — 属性 → 41記号への射影（`lib/vision/resolve.ts`）

テンプレートの分類結果と、カウンタの下線・点が食い違ったら、**カウンタを信じて記号を探し直す**
（カウンタの方が精度が高く、過大予測を出さないため）。
存在しない組み合わせなら「近い記号へ丸めずに unknown」。

### Stage 5 — ピッカーへのプリフィル

認識結果は**答えではなく下書き**。`mobile/App.tsx` はピッカーに流し込むだけで、
確信度が low のものは理由つきで警告を出す。

---

## 検証の状態

| 対象 | 状態 |
|---|---|
| lib/vision の TS 実装 ⇔ Python 参照実装 | **合成2322枚で bars/dots/最近傍テンプレートが完全一致**（`node tools/verify_ts.cjs`、最大相関差 9.4e-14） |
| Stage 1〜4 の通し（合成タグ帯） | 60本で実行。切り出し100%（`tools/SCAN.md`） |
| **Stage 1〜4 の通し（実物のタグ）** | **20枚122記号で 再現率 91.0% / 確定分の正解率 91.7%**（`node tools/score_real.cjs`） |
| 処理時間 | 20枚46秒（1枚2.3秒）。PCでの値で、実機は未測定 |
| 型チェック / lint / Web ビルド | 通過 |
| **実機での動作** | **未確認**（この環境に iOS 実機・シミュレータが無い） |
| **記号SVGの形状の正確性** | 公式リーフレットと照合済み（`tools/OFFICIAL_MATCH.md`）。一致度の低いものが残る |

### この数字の読み方（過大に読まないために）

- **43記号のうち25種しか評価されていない。** 平干し系の日陰なし（`420/410/415/430/435`）、
  タンブル高温（`320`）、アイロン高温（`530`）、40℃/30℃以外の洗濯温度は**1件も出ていない**。
- **日陰の負例は全体で1件しかない。** 「日陰と答えておけば93.8%」という状態と
  区別がつかない。閾値は合成データから決めている。
- **足切りの値（相関0.23）は、この122記号に合わせて選んだもの。** 写真が増えたら振り直す。
- 実機で撮り直すと結果は動く。同じタグを5回撮った測定では29記号中±3記号（約10ポイント）
  振れた。**2記号未満の差は撮り方のばらつきと区別できない**（機械採点は決定的なので、
  同じ写真・同じコードなら毎回同じ数字になる。振れるのは撮り直したとき）。

## 次にやること（優先順）

1. **写真を増やす。** いま精度を上げるのに最も効くのはコードではなくデータ。
   優先順は ①下線を持つ記号（`151` `152` `611` `711` など。最大の誤りの塊）
   ②日陰でない自然乾燥（負例が1件しかない）③一度も評価されていない18種。
2. **作業解像度を上げて下線が救えるか測る。** 1400px では、元写真で分離している
   下線2本が潰れて1本に見える個体がある。1400 / 1800 / 2200 で `bars` を測る。
   効くなら `mobile/` 側の縮小も直す。
3. **撮影ガイドに「記号の高さの3割の余白を四辺に」を入れる。** 枠が下線を切ると、
   最も危険な向きの誤りになる。
4. 実機で `npx expo start` を通し、撮影→読み取りが動くことと処理時間を確認する。
5. 禁止記号どうしの取り違え（`600` を `100`／`300` と読む）。×が面積を支配する
   問題で、シルエット・穴の形はどちらも測って外れている。別の筋が要る。
