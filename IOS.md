# iOS 実装プラン — 測定にもとづく設計

このドキュメントは意見ではなく、`tools/` の測定結果から導いた実装要件。
数字の出所は [tools/BENCHMARK.md](tools/BENCHMARK.md) / [tools/RESOLUTION.md](tools/RESOLUTION.md) /
[tools/TEMPLATE_MATCH.md](tools/TEMPLATE_MATCH.md)。再現手順は [tools/README.md](tools/README.md)。

---

## 測定でわかったこと（結論から）

### 1. この問題の8割は、学習モデルなしで解ける

41個のクリーンテンプレートとの正規化相互相関（NCC）だけで、劣化画像に対して:

| 何を当てるか | 精度（1記号120px時） |
|---|---|
| 基本形（桶/三角/□/□に○/アイロン/円） | **97.9%** |
| 下線の本数（通常/弱い/非常に弱い） | **99.1%** |
| 点の個数（乾燥温度・アイロン温度） | **100.0%** |
| 41クラス完全一致 | 80.5% |

テンプレートはフォント0、テストはフォント1・2から生成しているので、
**同一ビットマップ同士の照合ではない**（字形の汎化を測っている）。

### 2. 残る誤りは、ほぼ全部「記号の中の文字」

120px での基本形別の誤り率:

| 基本形 | 誤り率 |
|---|---|
| **tub（洗濯桶）** | **42.9%** |
| circle（ドライ/ウェット） | 9.7% |
| natural（自然乾燥） | 9.4% |
| triangle（漂白） | 8.3% |
| tumble（タンブル乾燥） | 4.2% |
| **iron（アイロン）** | **0.0%** |

そして中身を見ると `190→150` `180→150` `160→180` `140→180`（温度数字の取り違え）、
`620→610` `621→611`（P と F の取り違え）が上位を占める。
**下線の本数は保たれたまま数字だけ間違う**（`161→151`、`142→152`）。

> つまり、学習モデルが必要なのは「桶と円の中の文字」だけ。
> 数字6クラス（30/40/50/60/70/95）と文字3クラス（P/F/W）。
> 「AIで洗濯タグを読む」の"AI"は、この小さな一点に集約できる。

### 3. 律速は解像度、しかも下線が最も脆い

ぼけ・コントラスト・ノイズ・回転を固定して解像度だけ振った結果:

| 1記号のpx | 下線 正解率 | 点 正解率 |
|---|---|---|
| 200〜100px | **100.0%** | 100.0% |
| 90px | 97.8% | 100.0% |
| 80px | 94.6% | 100.0% |
| 70px | 83.3% | 100.0% |
| 60px | 74.6% | 100.0% |
| 50px | 61.2% | 97.6% |

「常に0と答える」だけのベースラインが下線 56.5%。**60px 以下は事実上そこに落ちている**
（`tools/BENCHMARK.md` の混同行列では s4/s5 で 1本・2本が全て0本と判定され、
正解率56.5%はベースラインと完全に一致する。精度が残っているのではなく死んでいる）。

点（dots）は50pxでも100%。**下線が2本の水平線という最も細い特徴なので、ここが律速する。**

### 4. 【最重要】誤りは100%が過小方向

全解像度・全条件で、**過大予測（実際より多く数える）は 0.0%**。
下線を「無いのに有る」と誤ることは一度も起きなかった。誤りは常に「有るのに無いと言う」。

これは安全設計にそのまま使える性質:

- 「下線を検出した」→ **信用してよい**
- 「下線を検出しなかった」→ **信用してはいけない**（解像度が足りないだけかもしれない）

しかも「下線なし＝通常の洗濯でよい」は**最も危険な誤り**（実際は弱い洗濯指定なのに通常コースで回す＝生地を傷める）。
したがって:

> **解像度が閾値を下回るときは `bars=0` を返してはならない。`unknown` を返す。**

これは推測ではなく、混同行列から直接導かれる実装規則。

---

## 実装アーキテクチャ

### Stage 0 — 撮影（AVFoundation）

測定から出た**ハード要件**:

- **1記号あたり 100px 以上**。マージンを見て **150px を目標**にする
- 画面にガイド枠を出し、タグを枠いっぱいに収めさせる

現実性の見積り: iPhone の12MP（4032×3024）で、幅60mmの視野を撮ると約67px/mm。
洗濯タグの記号は実寸6〜10mm角なので **400〜670px** になる。
つまり**タグに寄れば要件は余裕で満たす**。失敗するのは「服全体を撮る」ケースだけ。
→ ガイド枠とリアルタイムの「近づいてください」表示で構造的に防ぐ。

- **ピント判定**: クロップ領域のラプラシアン分散でシャープネスを測り、閾値未満はシャッターを切らせない
- `AVCaptureDevice.focusMode = .continuousAutoFocus` + マクロ対応機種では近接撮影

### Stage 1 — タグと記号列の検出（Vision）※ここが唯一の難所

```swift
VNDetectRectanglesRequest      // タグの外形
CIFilter.perspectiveCorrection // 台形補正
VNDetectContoursRequest        // 記号の輪郭（iOS 14+）
```

**記号列は「ほぼ同じ高さの図形が横一列に等間隔で並ぶ」**という非常に強い事前知識がある。
輪郭を高さとベースラインでクラスタリングすれば、記号の切り出しはルールベースで足りる。

**下流（Stage 2〜3）は測定済みで解けている。工数と失敗リスクはこの Stage 1 に集中する。**
プロトタイプではここを手動クロップで代替し、先に全体を通すのが正しい順序。

### Stage 2 — 記号ごとの分類（Accelerate、学習なし・オフライン）

Python の参照実装（`tools/features.py` / `tools/match.py`）をそのまま Swift に移す。
移植用のコアは [ios/CareSymbolReader.swift](ios/CareSymbolReader.swift) に用意した。

1. 背景の平坦化（重いガウシアンで照明を推定して除算＝フラットフィールド補正）
2. 大津の二値化
3. 連結成分ラベリング（union-find）
4. 下線本数 = 輪郭より下にある「幅広・薄い」成分の行プロファイルのピーク数
5. 点の個数 = 輪郭内部の「小さく・丸く・詰まった」成分の数
6. 基本形 = 41テンプレートとの NCC（`vDSP_dotpr`）

**推論コストゼロ、通信ゼロ、オフライン動作、決定的**。41テンプレート×3584次元の内積だけ。

### Stage 3 — 文字だけを読む（唯一の学習パート）

桶・円の内側だけを切り出して:

- **案A**: `VNRecognizeTextRequest`（`.accurate`）→ 結果を閉集合 `{30,40,50,60,70,95}` / `{P,F,W}` にスナップ。
  集合外なら `unknown`。追加の学習コストゼロ。まずこれを試す。
- **案B**: Core ML の小さな分類器（数字6クラス／文字3クラス）。
  学習データは `tools/degrade.py` でいくらでも作れる（すでに生成器がある）。
  Create ML の画像分類器で数百KB、オンデバイスで即答。

案Aで足りるかは未測定。ここが次に測るべき点。

### Stage 4 — 属性 → 41記号への射影（制約充足）

属性（基本形・温度・下線本数・点数・×の有無）が揃ったら、**実在する41通りへ射影**する。
存在しない組み合わせ（例: 桶＋点2個）は機械的に弾く。

> **弾いたときに「最も近い実在の記号」へ丸めてはならない。`unknown` にする。**
> 丸めた瞬間に、根拠のない断定が混入する。

### Stage 5 — ピッカーへのプリフィル

認識結果は**答えではなく下書き**。Web版（`components/Picker.tsx`）と同じ
「1分類1記号」のピッカーに流し込み、人が確定する。
`unknown` の分類はハイライトして「ここは読めなかったので選んでください」と出す。

---

## VLM（Claude vision 等）はどこで使うか

上の設計だと、**記号の読み取りにVLMは要らない**。使うとすれば:

1. **Stage 1 の代替**（タグの位置と記号列の特定）— ここは古典CVが弱く、VLMが強い
2. **Stage 3 の代替**（文字の読み取り）— ただしオンデバイスより遅く、通信が要る
3. **フォールバック**: 古典CVが `unknown` を返したときだけ問い合わせる

3番が費用対効果として最も筋がよい。**常時APIを叩く設計にすると、
オフライン動作・レイテンシ・コストの全部を失って、精度はほとんど上がらない**。

---

## この測定が示していないこと（過信しないための明記）

- **実物のタグでの精度は未測定**。合成劣化はぼけ・解像度・コントラストへの感度を測るものであって、
  実写の性能を保証しない。刺繍タグ・光沢生地・折れ・洗濯による退色・くしゃくしゃのタグは未評価。
- **Stage 1（検出・矩形補正）は未実装・未測定**。工数とリスクはここに集中する。
- **記号SVGの形状の正確性が未検証**（README 参照）。テンプレートが間違っていれば
  マッチングの精度は測っても意味がない。**公式記号一覧との照合が全ての前提。**
- 文字認識（案A/案B）の精度は未測定。

## 次に測るべきこと（優先順）

1. **記号SVGを公式一覧と照合**（これが崩れると他が全部無意味）
2. 実物のタグを50〜100枚撮って評価セットを作る
3. Stage 3（文字認識）を `VNRecognizeTextRequest` で測る
4. Stage 1（検出）のプロトタイプ
